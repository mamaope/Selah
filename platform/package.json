{
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "jsdom": "^29.1.1",
    "playwright": "^1.48.0"
  },
  "scripts": {
    "test": "node engine/engine.test.js && node web/ui.test.js",
    "build": "node build.js",
    "e2e": "playwright test",
    "e2e:install": "playwright install --with-deps chromium"
  }
}
